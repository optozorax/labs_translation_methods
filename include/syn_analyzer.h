#include <vector>
#include <string>
#include <tables.h>

std::vector<std::vector<Token>> Analyzer(const std::vector<Token>& tokens, Tables& tables, string path, std::vector<std::string>& ident, std::vector<std::string>& con);